# アーキテクチャと技術選定

対象: MeetingRecorder（Windows 11 向けポータブル会議録音アプリ。文字起こしは録音後にローカルで実行）

---

## 0. 設計の最優先事項

要件で定められた優先順位をそのまま実装の制約にしています。

```
1. 録音の確実性
2. マイク音声・PC内部音声を正しく保持すること
3. 保存音声を文字起こしに適した状態で保持すること
4. 録音後の文字起こし精度
5. 操作の分かりやすさ
6. 議事録生成等の付加機能
```

**リアルタイム性はこの一覧に存在しません。** これは以前の版からの最大の変更で、
本ドキュメントのほぼすべての設計判断がここから導かれます。理由は一文です。

> 文字起こしは録音後に何度でも実行できる。しかし録音データを失えば復元できない。

この順位から導かれる不変条件（コード上の全設計判断の根拠）:

| 不変条件 | 実装上の担保 |
| --- | --- |
| 録音中に音声認識を実行しない | `RecordingPipeline` は `ISpeechRecognizer` を引数にも取らず、フィールドにも持たない。テスト `StartingARecordingNeverTouchesASpeechRecognizer` がリフレクションで両方を検査する |
| 録音開始のクリティカルパスにモデルロードを置かない | 認識器を生成する箇所はアプリ全体で `AppServices.TryCreateTranscriptionRecognizer` の1つだけで、呼び出し元は文字起こしコマンドのみ |
| 録音停止で文字起こしを始めない | `MeetingSessionManager.Stop` は会議フォルダーを完成させて戻るだけ。テスト `StoppingDoesNotStartATranscription` が保証 |
| 停止ボタンを押さなくてもデータが残る | WAV はヘッダ自己修復方式で逐次書き込み、認識結果は追記専用ジャーナルへ逐次追記 |
| 片方のデバイスが落ちても録音は続く | 各キャプチャは独立。失敗側は無音として合成され、警告を表示 |
| 発言元は推測しない | アプリ録音では系統別ファイルから、インポートでは「インポート音声」1種類。どちらもAI推測ではない |
| 音声に時間変動・非線形な加工をしない | ゲート／AGC／コンプ／リミッターは実装ごと削除。テスト `RemovedProcessingStagesTests` がアセンブリ内に型が存在しないことを検査 |
| 管理者権限を要求しない | WASAPI 共有モード、`SetThreadExecutionState`、ユーザー領域への書き込みのみ |

---

## 1. 技術選定

### 1.1 評価軸

要件どおり、以下を最重要評価基準としました。

```
Windows 11 / 管理者権限不要 / WASAPI Loopback / 長時間安定録音 /
ポータブル配布 / CPUローカルSTT（録音後） / GitHub Actions で再現可能なビルド /
保守性 / ライセンス
```

### 1.2 選定結果

| 領域 | 採用 | 主な理由 | 検討した代替案と不採用理由 |
| --- | --- | --- | --- |
| 言語 / ランタイム | C# / .NET 8 | WASAPI へ P/Invoke なしで到達でき（NAudio）、自己完結型 publish が標準機能、GitHub Actions の `windows-latest` に SDK が同梱 | C++/WinRT: 開発・保守コストが高い。Electron + Node: ランタイム同梱で 200MB 超、ネイティブ音声取得に追加バインディングが必要。Python: 配布に PyInstaller 等が必要で、要件の「Python 事前インストール禁止」を満たすのが難しい |
| UI | WPF | .NET 8 に同梱、単一 EXE 不要のポータブル配布と相性が良い、日本語 IME と高DPIに実績 | WinUI 3: Windows App SDK の再配布が必要でポータブル配布が複雑になる。WinForms: レベルメーター等の描画が古く保守性が低い |
| 音声取得 | WASAPI（NAudio 2.2.1 経由） | 共有モードのループバックは追加ドライバ・管理者権限なしで「再生デバイスの出力全体」を取得できる唯一の実用手段 | Stereo Mix: 搭載はサウンドカード依存で現代のノートPCにはほぼ無い。VB-CABLE 等の仮想デバイス: インストーラーと管理者権限が必要（要件で禁止）。プロセス単位ループバック (ActivateAudioInterfaceAsync + PROCESS_LOOPBACK): 特定アプリ専用になり「PC再生音全体」という要件に反する |
| 音声認識 | whisper.cpp（Whisper.net 1.8.1） | CPU のみで動作、量子化モデルでメモリ帯域律速の省電力CPUに適合、ggml モデルは MIT、ネイティブバイナリが NuGet で配布されビルドが再現可能、日本語性能が実用水準 | Vosk: 日本語モデルの精度が Whisper small に届かない。Azure/Google/AWS STT: クラウド送信となり要件違反。ONNX Runtime + Whisper ONNX: 変換済みモデルの配布・ライセンスが不透明でビルド再現性が低い |
| MP3 エンコード | Windows Media Foundation（NAudio 経由） | OS 同梱のためバイナリ追加ゼロ、LGPL 義務が発生しない | LAME / NAudio.Lame: LGPL のネイティブ DLL 同梱が必要。要件を満たせないわけではないが、社内配布のライセンス確認コストを避けた |
| 音声インポート | Windows Media Foundation（NAudio 経由）+ 自前 WAV リーダー | MP3 / M4A / AAC を OS 同梱のコーデックで復号でき、配布物に何も足さない。WAV は自前リーダーで扱うため OS コーデックに一切依存しない | FFmpeg 同梱: 配布サイズとライセンス確認が増える。FFmpeg 別途インストール要求: 「事前インストール不要」という製品の前提を壊す。マネージド MP3/AAC デコーダー: ライセンス確認とネイティブ依存が増える |
| 話者分離 | 自前実装（MFCC + オンラインクラスタリング） | 追加ダウンロードゼロ、CPU 負荷が小さく、実行タイミングを文字起こし時に移せる | pyannote / WeSpeaker 等のニューラル埋め込み: 数百MBの追加ダウンロードとライセンス確認が必要で、優先度9の機能としては過大 |
| ローカルLLM | llama.cpp（LLamaSharp 0.24.0）+ Qwen2.5-1.5B-Instruct GGUF | CPU 推論、Apache-2.0 で社内商用利用の可否が明確、日本語性能がサイズ比で良好、GGUF 公式配布あり | Llama 3.2: ライセンスが Llama Community License で商用条件の確認が必要。Qwen2.5-3B 以上: 研究用ライセンスまたは基準PCで実用速度に届かない |
| テスト | xUnit | .NET 標準的、`dotnet test` で CI 統合が容易 | - |

### 1.3 プロジェクト構成

```
src/
  MeetingRecorder.Core         net8.0        ← プラットフォーム非依存。DSP・パイプライン・永続化・STT抽象
  MeetingRecorder.Audio        net8.0-windows ← WASAPI / Media Foundation / スリープ抑制
  MeetingRecorder.Stt          net8.0        ← whisper.cpp アダプタ
  MeetingRecorder.Diarization  net8.0        ← MFCC + クラスタリング
  MeetingRecorder.Minutes      net8.0        ← 抽出型 + ローカルLLM議事録
  MeetingRecorder.App          net8.0-windows ← WPF
tests/
  MeetingRecorder.Core.Tests   net8.0        ← DSP/パイプライン/永続化/話者分離/議事録
  MeetingRecorder.Stt.Tests    net8.0        ← モデルダウンロード整合性・認識器契約
  MeetingRecorder.Audio.Tests  net8.0-windows ← WASAPI 層（実機依存部分は明示的に対象外）
  MeetingRecorder.App.Tests    net8.0-windows ← WPF 画面ロード・データバインド検証
```

**`Core` を `net8.0`（Windows 非依存）にしている理由**が設計上もっとも重要です。
録音パイプライン全体が `IAudioCaptureSource` / `IAudioFileWriter` / `ISpeechRecognizer`
というインターフェース越しに動くため、マイクもスピーカーも存在しない CI ランナー上で
「2ストリームを合成して1ファイルに書く」という中心的な振る舞いを丸ごとテストできます。

---

## 2. 音声パイプライン

### 2.1 スレッド構成

**録音中**（これがすべてです）:

```
[WASAPI capture callback]  ── mono float 変換 → レベルメーター → リングバッファ書き込み
   (OSスレッド, 最短経路)              │
                                       ▼
[pump スレッド AboveNormal] ── リング読み出し（壁時計 -250ms／不足分は無音補間）
                              → DCオフセット除去
                              → 16kHz へリサンプル → 系統別 WAV 書き込み
                              → 一定ゲインで加算 → ミックス WAV 書き込み
                                       │
                                       ▼
[UI スレッド]               ── 100ms ごとに状態をポーリング（Dispatcher 経由で反映）
```

pump の1ティックあたりの処理は、サンプルごとの数回の積和と2本のシーケンシャル書き込みだけです。
**推論もクラスタリングもアロケーションもこの経路にはありません。**
以前ここにあった VAD / チャンカー / STT キュー / STT ワーカーは、
すべて削除されました。

**文字起こし中**（ユーザーが押したときだけ・録音は行われていません）:

```
[スレッドプール]  ── モデルロード → WavFileReader で系統別ファイルを走査
                     → 無音位置で最大27秒に分割 → whisper.cpp 推論（beam 5）
                     → 話者クラスタリング → TranscriptStore へ
                             │
                             ▼
[UI スレッド]      ── Progress<T> 経由で進捗、完了時に本文を差し替え
```

AI 推論は UI スレッドでもキャプチャスレッドでも実行されません。
そして**キャプチャスレッドが存在する間は、どこでも実行されません。**

### 2.2 内部フォーマット

**48 kHz / モノラル / float32** を内部の正準フォーマットとしています。

- Windows 11 の WASAPI 共有モードは既定で 48 kHz。ここに合わせると再サンプルが 1 回で済みます。
- モノラル化はキャプチャコールバック内で実施（会議記録にステレオ定位は不要で、
  ファイルサイズと前処理コストが半分になります）。
  複数チャンネルは既定で単純平均です。ただし差動ペアを生で出すエンドポイントでは
  平均化が信号を打ち消すため、`MonoDownmixer` が開始直後の数秒だけエネルギー比を測り、
  無相関チャンネルの理論値（-10·log10(N) dB）より 6 dB 以上低い場合にのみ、
  そのストリームをチャンネル 0 に切り替えます（§2.2.1）。
- サンプルレート変換の補間は **Catmull-Rom（3次）**。線形補間は振幅特性が
  sinc²(f / f_source) で、44.1→48 kHz で 8 kHz を 0.95 dB、16→48 kHz で 6 kHz を
  3.77 dB 落としていました。Catmull-Rom はそれぞれ 0.16 dB / 2.31 dB です
  （`ResamplerFrequencyResponseTests` が掃引正弦波で実測）。
  コストは 1 出力サンプルあたり乗加算 2 回 → 4 回、履歴 1 → 3 サンプルのみで、
  ポリフェーズ sinc は録音コールバックを重くするため採用していません。
- **PC内部音声は通常この変換を通りません。** レンダーエンドポイントは 48 kHz で
  動作するため素通し（`Resampler.IsPassThrough`）です。変換を通るのはマイク側だけで、
  これが「同じファイル内でマイクだけがこもる」ときの最初の容疑者になります。
- WAV 出力は 48 kHz / 16bit / モノラル = 96,000 B/s ＝ **345.6 MB/時**（約 346 MB/時）。
  MP3 は既定の 192 kbps で 24,000 B/s ＝ **86.4 MB/時**。
- **既定の保存形式は MP3 192 kbps です。** 会議ファイルは人が聴くためのもので、
  文字起こしはこれを読みません（Whisper が読むのは 16 kHz の `recognition-*.wav`）。
  ドライブが静かに埋まる原因は、意識して選んだ設定ではなく誰も見なかった設定です。
- ディスク使用量の計算は `RecordingFootprint` の 1 箇所に集約しています。
  以前は事前チェックが `meeting.wav` の分しか数えておらず、作業音声 2 系統
  （各 32,000 B/s）を無視していたため、**実際に録音できる時間の 1.67 倍**を
  表示していました（96,000 対 160,000 B/s）。

### 2.2.1 キャプチャ形式の記録

音質の問題は原因がアプリ側とは限らないため、**推測ではなく記録**します。

`WasapiCaptureSource.StartCore` は、ストリームが開いた瞬間に Windows が実際に返した
形式を `CaptureFormatReport` にまとめ、そのままログへ書きます。

```
Microphone capture format:
  Device           = Headset Microphone (USB Audio Device)
  SourceFormat     = 16000 Hz / 1 ch / 16-bit Pcm
  WASAPI MixFormat = 16000 Hz / 1 ch / 16-bit Pcm
  InternalFormat   = 48000 Hz / mono
  Conversion       = resampled 16000 -> 48000 Hz
  Source Nyquist   = 8000 Hz
  NOTE             = the endpoint itself is band-limited; ...
```

`SourceSampleRate <= 16000` のとき、`UserWarning` が警告として上がり、
`metadata.json` の警告一覧にも入ります。**アップサンプリングで復元できたとは
決して表示しません。** 8 kHz より上の音は最初のコールバックが来る前に失われています。

録音停止後には `SpectralBalance` が両方の作業用音声を同じ帯域分割
（300-1k / 1-2k / 2-4k / 4-7 kHz）で測り、比較をログへ書きます。
マイク側の最上帯域が PC内部音声側より **6 dB 以上**低いときにだけ警告を出し、
それ未満では何も主張しません。録音が閉じたあとのファイルに対して実行するため、
録音経路には一切影響しません。

### 2.3 マスタークロックと同期（要件24）

マイクと再生デバイスは別々の水晶発振器で駆動されるため、そのまま結合すると
1〜2時間で数百 ms のずれが蓄積します（民生機の典型値 50〜100 ppm ＝ 2時間で 0.36〜0.72 秒）。

対策:

1. **壁時計をマスターにする。** pump は `Stopwatch` の経過時間から
   「今までに何サンプル出力すべきか」を求め、その分だけ各ストリームから読み出します。
2. **ジッターバッファを常に持つ（`JitterBufferSeconds` = 250 ms）。** pump は
   壁時計より常に 250 ms 遅れて読み出します。**これが無いと録音に穴が開きます。**
   エンドポイントは壁時計どおりには配信せず、バッファ単位でまとめて渡してくるためです
   （WASAPI 100ms バッファ ＋ OS のスケジューリング揺らぎ）。手持ちのバッファが
   ゼロのまま壁時計で読み出すと、配信の谷がそのまま無音として書き込まれます。
   250 ms は 100 ms のバッファと通常のジッターを吸収でき、かつ
   `DriftCompensator` が元から前提としていたバックログ量と一致します。
   録音が遅れて始まるわけではありません。**取りこぼしはゼロで、
   出力タイムラインは 1 秒あたり 1 秒で進みます。**
3. **停止時はリングを空にしてから終わる。** pump は常に 250 ms 遅れているため、
   そのまま止めると会議の最後の 250 ms を捨てることになります。停止処理は
   キャプチャを止めたあと、リングに残っているものを（壁時計ではなく残量に従って）
   すべて書き出してから pump を終了させます。
4. **不足分は無音を挿入する。** WASAPI ループバックは再生が完全に無音の間、
   パケットを一切返さないことがあります。この場合も出力タイムラインは壁時計どおりに進み、
   録音長と文字起こしのタイムスタンプが実時間とずれません。
5. **恒常的な余剰はごく少量ずつ捨てる。** `DriftCompensator` は
   平滑化したバックログが「目標250ms + 閾値150ms」を **5秒以上継続** した場合にのみ、
   1 tick あたり最大 1 ms 分を破棄します。無音後のバースト到着を
   ドリフトと誤認しないための時定数です（テスト `ABurstAfterSilenceIsNotMistakenForDrift`）。
6. **マイク側が痩せたら黙らず報告する。** ループバックと違い、マイクは
   「無音でも配信は続く」のが正常です。マイク系統に挿入した無音が録音全体の
   0.1% を超えた場合、その秒数と割合を警告として残します
   （テスト `AStarvedMicrophoneIsReportedRatherThanHidden`）。

補正上限は 1 tick(20ms) あたり 1 ms ＝ 最大 5% のクロック誤差まで吸収でき、
実機の誤差（0.01% 程度）に対して 2 桁以上の余裕があります。

**この設計は実測で決めました。** ジッターバッファが無い状態で、実機と同じ
バースト配信（100 ms 単位・遅延あり）を与えると、3.3 秒の録音のうち
**323 ms（約10%）が挿入された無音**になり、100 ms 窓 23 個のうち 2 個が
壊れていました。ジッターバッファ導入後は同条件で**挿入無音 0 ms・欠落 0 個**です
（テスト `ABurstyEndpointStillProducesContinuousAudio`）。

### 2.3.1 キャプチャバッファ長

マイクは `WasapiCapture(useEventSync: true, audioBufferMillisecondsLength: 100)` で取得します。
**以前は 20 ms でした。** WASAPI は遅れたクライアントを待たず、バッファが埋まると
古い音声を上書きします。20 ms はマネージドコールバックがスケジュールされるまでの
猶予として短すぎ、会議クライアントと同時に動く実機では取りこぼしの原因になります。
短いバッファを必要とする理由も無く（パイプライン側に 30 秒のリングがあり、
レベルメーターは同じコールバックで更新され、モニタリング再生も行いません）、
NAudio の既定値である 100 ms に戻しました。

### 2.4 音声処理の方針

**このアプリが音声に対して行う処理は、以下がすべてです。**

| 対象 | 処理 |
| --- | --- |
| 認識用音声（`recognition-*.wav`） | モノラル → 48kHz へリサンプル → DCオフセット除去 → 16kHz へリサンプル → 16bit PCM |
| 保存音声（`meeting.wav`） | 同上（16kHz 変換の代わりに）→ *[任意] マイク系統のみ固定シェルビング 1 段* → 各系統に一定 -8.18 dB → 加算 → **録音終了後にファイル全体へ一定ゲイン1回** |

以前の版にあった **ソフトノイズゲート / ラウドネス正規化(AGC) / コンプレッサー /
リミッターは、無効化ではなく実装ごと削除**しました。

**なぜ削除したか。** これらはいずれも「ゲインを時間の関数として変える」処理です。
一方 Whisper が特徴量として読んでいるのは、まさにその**振幅包絡の形**です。
つまり人間の耳のための加工が、そのまま認識器への入力を歪めます。影響は
**小さな声ほど大きく**なり、本製品が対象とする会話音声で最も損なわれます。

保存音声にも適用しない理由は、この録音の主用途が「あとから文字起こしすること」
だからです。音楽的・放送的に聞きやすい音声を作ることは目的ではありません。

削除されたことは `RemovedProcessingStagesTests` がアセンブリ内の型名と
`AudioProcessingSettings` のプロパティ名の両方で検査しています。
「設定で無効化されている」ではなく「存在しない」ことを検査するのは、
無効化された段は設定ひとつで復活してしまうためです。

#### 唯一の任意処理: マイクのリスニング EQ（`ListeningEq`）

帯域は足りているのにマイク側だけが PC 音声側より鈍く聞こえる場合、それは
**スペクトルの傾き**であり、傾きは固定フィルターで正直に補正できる唯一の対象です。
`AudioProcessingSettings.MicrophoneListeningEqEnabled`（**既定オフ**）で有効化します。

| 制約 | 内容 |
| --- | --- |
| 何であるか | 係数固定の 2 次ハイシェルフ 1 段（既定 2.5 kHz / 4 dB） |
| 何でないか | 閾値・検出器・包絡・履歴を持たない。`y = h * x` が固定の `h` で成立するので、小声と大声の振幅関係は不変 |
| どこに掛かるか | **保存音声のマイク系統のみ**。作業用音声を書き出した**あと**に適用 |
| どこに掛からないか | 認識用音声、および PC内部音声系統（問題のある系統ではない） |
| クリップしないこと | フィルター全体を自身のインパルス応答の **L1 ノルム**で正規化。したがって任意の入力に対し `max|y| <= max|x|` が成立し、「2系統フルスケール → 合計ちょうどフルスケール」の保証が壊れない。実装上は「高域を上げる」ではなく「低中域を下げる」形になり、その分は録音終了後の固定ゲインが返す |
| 有効化の根拠 | §2.2.1 の `SpectralBalance` 実測。感覚で入れる値ではない |

デバイス自体の帯域が足りない場合、このフィルターは**何も復元しません**。
そのケースは §2.2.1 の警告が先に出ます。

### 2.5 音量調整（単一固定ゲイン）

```
peak = max |input[n]|                          （ファイル全体を走査）
gain = min( 10^(target/20) / peak , 10^(max/20) )
output[n] = input[n] * gain                    （全サンプルに同じ gain）
```

`gain` は `n` に依存しません。したがって**線形かつ時不変**であり、話し始めの小さい音・
子音・小声・大声・無音・発話のダイナミクスの相対関係は一切変化しません。
テスト `EveryNonZeroSampleIsMultipliedByTheSameConstant` が、
非ゼロサンプルに対する `output[n] / input[n]` が許容誤差内で一定であることを検査します。

| 定数（`AudioProcessingSettings`） | 値 | 根拠 |
| --- | --- | --- |
| `DcBlockerCutoffHz` | 20 Hz | 成人男性の最低基本周波数（約80 Hz）の1オクターブ下。DCオフセットはヘッドルームを消費して後段の固定ゲインを小さくし、ピーク測定も歪めるため残している |
| `MixGainPerStreamDb` | **-8.1787 dB（= 0.39 倍）** | ミキサー手前で起こり得る最大ゲインからの逆算。§2.5.1 を参照。リミッターもクランプも使わずにクリップを防ぐという性質は変わらない。失った分は録音終了後の固定ゲインで返す |
| `NormalizationTargetPeakDbFs` | **-1.0 dBFS** | PCM のサンプルピークは、DA変換や非可逆圧縮でサンプル間に再構成される真のピークと一致しない。ITU-R BS.1770-4 / EBU R 128 が同じ理由で -1 dBTP の余裕を推奨しており、同じ値を同じ理由で採る。16bit 量子化の丸めもここに吸収される |
| `MaxNormalizationGainDb` | **+20.0 dB（= 10倍）** | これ以上増幅しても持ち上がるのは発話ではなく収録時のノイズフロア。ノイズを10倍より大きくしても会議は聞き取りやすくならない。ゲインを一定に保ったまま「大きくしすぎない」という制限であり、リミッターではない |
| `NormalizationSkipThresholdDb` | 0.1 dB | 約1.2%。可聴でなく、16bit へ再量子化する価値もない差。この場合はファイルを書き直さないため、pump が書いたバイト列がそのまま残る |
| `ClipDetectionThreshold` / `ClipDetectionSampleFraction` | 0.999 / 0.0001 | 1時間の録音で数サンプルがフルスケールに触れるのは偶然であって歪みではない。1万分の1（サンプルレートによらず、1時間あたり 3600 × 0.0001 = 約0.36秒分のサンプル）は、狼少年にならず、かつ実際に過大入力された録音は捕捉できる境界 |

**クリッピングはゲインを先に決めることで防ぎます。** サンプルループの中に閾値判定は
存在せず、大きすぎた値を潰す段もありません（`AHotFileIsScaledDownRatherThanHavingItsPeaksFlattened`）。
`WavFileWriter` に残るクランプは NaN や異常値に対する算術的な最後の砦であり、
クリッピング対策ではありません。

**書き直しの安全性。** 正規化は隣に一時ファイルを書いてから置換します。
途中でクラッシュしても、ディスクが埋まっても、pump が書いた録音がそのまま残ります。
音量調整を失うのは残念ですが、会議を失うのは許容できません。

**元音声が既にクリップしている場合**は、ログ・警告・`metadata.json` の
`sourceClipped` に記録します。**「修復できた」とは表示しません。**
潰れたピークは後処理では復元できないためです。

### 2.5.1 ミキサーのヘッドルームはどこから来るか

以前は **-6.02 dB（0.5 倍）** でした。根拠は「2系統が同時にフルスケールでも
合計がちょうどフルスケール」で、これは**リサンプラが線形補間だった間は事実**でした。
2 サンプルの加重平均は値域を出られないからです。

補間を Catmull-Rom（3次）に変えた時点で、この前提は崩れました。

- Catmull-Rom は**制御点を通る**が、**制御点に挟まれる保証はない**。
- 基底関数は常に総和 1（定数は定数のまま）だが、サンプル間で 2 つが負になる。
- 有界入力に対する最大ゲインは**基底の絶対値の和**であり、サンプル間のちょうど中央で
  **正確に 1.25**（`Resampler.MaximumInterpolationGain`）。
- 到達する入力は特殊ではない。`[-1, +1, +1, -1]` — ナイキスト付近で折り返す
  フルスケール信号 — がそのまま 1.25 を出す。

0.5 のままなら、こういう 2 系統は 1.25 に合計され、`WavFileWriter` の
バックストップ・クランプで潰れます。**クリップを不可能にするための段が、
クリップの原因になる**という状態でした。

現在の定数はこの上界から導出しています。

| 段 | 有界入力に対するゲイン |
| --- | --- |
| Catmull-Rom 補間 | 1.25（実測・`ResamplerHeadroomTests`） |
| DC ブロッカー（ナイキスト） | `2 / (1 + R)` = 1.0013 |
| 合計（1系統） | 1.2516 |

必要条件は `1 / (2 × 1.2516) = 0.3995`。算術だけなら `1 / (2 × 1.25) = 0.4` ですが、
ちょうど 0.4 だと DC ブロッカーの分で合計 1.0013 となりクランプに触れるため、
**0.39** を採用しています（最悪ケース 0.976）。

**補間をクランプして有界にする**選択肢は採りませんでした。信号依存の非線形性を、
このアプリが「線形である」と主張している唯一の経路に入れることになるためです。
ヘッドルームは録音中のみのコストで、録音終了後の固定ゲインがそのまま返します。

**正直な限界。** これは各段のナイキスト以下でのゲインを賄うものであって、
DC ブロッカーの全有界入力に対する真の最悪値（L1 ノルム = 2.0、フルスケールの
レール間ステップでのみ到達）までは賄っていません。それは音響信号が作らない波形であり、
またこの変更以前から持っていた性質です。その領域は従来どおり writer のクランプが
バックストップです。

### 2.5.2 入力クリッピングの検出

`sourceClipped`（`metadata.json`）と「既に歪んでいます」警告は、**系統ごとに、
ゲインを掛ける前のキャプチャ音声**で数えています（`RecordingPipeline.CountClipping`）。

以前は完成したファイルを読み直していました。これが機能していたのは、
フルスケールの 2 系統が同相で入ったときに `0.5 + 0.5` がちょうど 1.0 になったからで、
**検出ではなく偶然**でした。実際には、

- 片方の系統だけが歪んでいる場合 → 混合後は半分以下。検出されない
- 2 系統が逆相で歪んでいる場合 → 打ち消し合う。検出されない
- ミキサー定数を何らかの理由で変更した場合 → 警告が黙って無効化される

現在は各系統で `|x| >= ClipDetectionThreshold` のサンプルを数え、
**どちらか一方**の系統で `ClipDetectionSampleFraction` を超えたら報告します。
両系統の平均ではありません。きれいな方の系統が壊れた方を隠さないようにするためです。

### 2.6 バックプレッシャーは存在しません

以前は録音と同時に認識を行っていたため、認識が遅れたときに音声をどう保持するかという
問題（`SttScheduler` のディスク退避）がありました。**録音中に認識を行わないため、
この問題そのものが存在しません。** `SttScheduler` と `.stt-spill/` は削除されました。

---

## 3. 音声認識（録音後のみ）

### 3.1 いつ動くか

**ユーザーが「文字起こし」を押したときだけ**動きます。それ以外に起動する経路は
ありません。認識器を生成するコードはアプリ全体で `AppServices.TryCreateTranscriptionRecognizer`
の1箇所で、その呼び出し元は `MainViewModel.TranscribeAsync` だけです。

- 録音開始時にモデルを読み込みません（読み込む理由がありません）。
- 録音中に推論しません。
- 録音停止で自動的に開始しません（`Stop` は会議フォルダーを完成させて戻ります）。
- 二重実行は、コマンドの実行可否と `WorkflowState` の両方で防いでいます。

対象は `OfflineTranscriptionService` に渡される 16 kHz の作業音声で、
アプリ録音なら系統別2本、インポートなら1本です。

### 3.2 発言元は録音の事実（アプリ録音の場合）

要件は「発言元は AI 推測ではなく録音元ストリームで決定する」ことを求めています。
そのため、認識が読むのは**混合ファイルではなく系統別の音声**です。混ぜてしまうと
発言元は推測に退化し、しかも**二度と復元できません**。系統別ファイルは録音中にしか
作れないため、録音中に 16 kHz で書き出しています（1系統あたり毎時約115MB）。

窓は `SpeechChunker` が無音位置で切り、生成時に発生元ストリームが刻印されます。
「マイク」「PC音声」は最後まで録音の事実であって、モデルの出力ではありません。

**インポート音声では音源が1本しかありません。** どちらがマイクでどちらが
PC音声かも、誰が話しているかも、その1本には記録されていません。したがって
`AudioSourceKind.Imported`（表示名「インポート音声」）を付けるだけで、
分類も話者クラスタリングも行いません。UI の「話者名を編集」も無効になり、
押した場合は理由を表示します。

### 3.3 認識に渡す音声は加工しない

**初期実装の誤りとその修正（経緯）。** 当初、認識器には録音ファイルと同じ音声、
つまりノイズゲート → ラウドネス正規化 → コンプレッサー → リミッターを通過した後の
音声を渡していました。実使用で「はっきり発音しないと認識されない」という報告があり、
原因はここでした。次にこれを分岐させ、認識側だけ DC 除去のみにしました。

**現在は、その分岐自体が不要になりました。** ゲート・正規化・コンプ・リミッターは
削除されたため、分岐すべき差がありません（2.4 節）。

```
キャプチャ → ミュート判定 → DC除去 ─┬─→ 16kHz リサンプル → 認識用 WAV（系統別）
                                      │
                                      └─→ 一定 -8.18 dB で加算 → 録音ファイル
                                              → 停止後にファイル全体へ一定ゲイン1回
```

`TheWorkingAudioIsTheCapturedAudioRatherThanAProcessedVersionOfIt` が、既知の
レベルのトーンを流し込んで、認識用ファイル側がそのレベルのまま届くことを検査します。

### 3.4 窓の切り方

| 設定 | 値 | 理由 |
| --- | --- | --- |
| 最大窓長 | **27 秒** | Whisper の受容野は 30 秒。これを超える窓はエンジン側で黙って切り捨てられるため、**切る位置をこちらで決め、必ず無音に置く** |
| **目標充填量** | **20 秒** | ここに達するまで無音では窓を閉じない。下記参照 |
| 無音フラッシュ | **800 ms** | 窓を閉じて**よい**位置の定義。閉じなければならない位置ではない |
| **無音の持ち越し上限** | **3 秒** | 目標未達のまま無音が続く場合の打ち切り。会話中の間より長く、話題の切れ目より短い |
| オーバーラップ | 200 ms | 無音ではなく長さで切らざるを得なかった窓の継ぎ目で、単語が両断されないように |
| プリロール | 300 ms | 語頭の子音を欠かないように |

#### 目標充填量が処理時間の最大のレバーです

whisper.cpp は渡された音声を必ず 30 秒までゼロ詰めしてからエンコーダーを回します。
**3 秒の窓と 26 秒の窓はエンコーダーにとって同じコスト**です。したがって
「発話の切れ目ごとに窓を閉じる」のは節約ではなく増幅で、60 回の間がある会議は
10 分の音声に対して 60 回分のエンコーダーパスを買うことになります。

同じ合成音声（4 秒発話 + 2 秒無音 × 100、計 10 分）での実測:

| 方式 | 窓の数 | 平均長 | エンコーダー換算 |
| --- | --- | --- | --- |
| 最初の無音で閉じる（変更前） | 100 | — | 3000 s（会議長の 5.00 倍） |
| 20 秒たまってから閉じる（変更後） | 25 | 23.7 s | 750 s（会議長の 1.25 倍） |

`TranscriptionWindowingTests.WindowsAreFilledInsteadOfEndingAtEveryPause` が
**両方の設定を同じ音声に対して実行して**比較しています（旧設定は
`SpeechChunker` のコンストラクタ既定値としてそのまま残っているため、
再現ではなく実物との比較です）。

窓の中の音声は**連続したまま**です。発話間の無音は残し、切り貼りしません。
そのためエンジンが返すタイムスタンプはそのまま録音上の時刻に対応します
（`EverySegmentStillLandsAtTheTimeItWasSpoken`）。
窓の末尾に持ち越された余分な無音だけは、渡す直前に `TrimCarriedSilence` が
`SilenceFlushMs` 相当を残して切り落とします。無音を大量に渡すことは、
Whisper が定型句を作り出す既知の条件のひとつでもあるためです。

精度への影響は「悪くならない、おそらく良くなる」方向です。Whisper は 30 秒窓で
訓練されており、長い窓は文脈が増えることを意味します。

短い数秒のチャンクを毎回文脈なしで処理する方式（旧・速報パス）は使いません。
**録音に追いつく必要がなくなったため、精度側に振れるようになった**というのが唯一の理由です。

### 3.5 話者分離の実行タイミング

話者クラスタリングは `OfflineTranscriptionService` の内部で、認識器の直後、
**同じスレッド**で実行されます。録音中には実行しません。
「あとからでもできる処理を、取り返しのつかないものが動いている最中に走らせない」
という原則をそのまま適用しています。

失敗しても「話者不明」に縮退するだけで、誤った話者を付けることも、
文字起こしを中断することもありません。

### 3.6 whisper.cpp の設定

| 設定 | 値 | 理由 |
| --- | --- | --- |
| `WithNoContext()` | 有効 | 1つのエンジンにマイク／PC音声の窓が交互に入るため、デコーダの文脈を持ち越すと「部屋の発言に条件付けされた、流暢だが誤った remote 側の文」が生成される。会議記録では最悪の故障モード。窓**内部**の文脈は失われず、窓は無音位置で受容野いっぱいまで取るため、文が必要とする文脈は失っていない |
| サンプリング | **プロファイル依存**（高速 1 / 標準 2 / 高精度 5） | §3.6.1 |
| `WithPrompt()` | 日本語の短い定型文 | プロンプトなしでは日本語が**句読点なしの一続き**で返ることが多く、会議記録として読み返しにくい。内容ではなく文体の指定であり、出力には現れない |
| `WithTemperatureInc()` | **プロファイル依存**（高速 0 / 標準・高精度 0.2） | 温度フォールバックは崩れたデコードを再試行して救う。時間がかかるのは救う必要があった箇所だけ |
| `NoSpeechProbability > 0.85` を破棄 | 有効 | Whisper は無音に対し「ご視聴ありがとうございました」等の定型句を出すことがある。文字列のブラックリストではなく **モデル自身が出す指標** で落とすため、正規の発話を誤削除しにくい |
| 最短入力 1 秒 | 無音パディング | whisper.cpp が 1 秒未満を扱えないため。短い返事を捨てずに認識させる |

### 3.6.1 速度/精度プロファイル

デコーダーの探索幅は 1 つの設定（`AppSettings.TranscriptionProfile`）で決まります。
ビーム幅と温度フォールバックを別々の設定として露出させると、意味のない組み合わせが
生まれ、誰も推論できない設定項目が 4 つになるためです。

| プロファイル | ビーム幅 | 温度増分 | 話者分離 |
| --- | --- | --- | --- |
| 高速 | 1（貪欲探索） | 0 | **常にオフ** |
| 標準（既定） | 2 | 0.2 | 設定に従う |
| 高精度 | 5 | 0.2 | 設定に従う |

- **高精度はプロファイル導入前と完全に同一**です。
  `SpeechRecognitionOptions.For(Accurate, …)` と `Offline(…)` の一致を
  `TheAccurateProfileIsExactlyWhatTheApplicationDidBefore` が検証しています。
  設定が現れたことで誰の文字起こしも劣化していない、という約束をコードで守るためです。
- 日本語プロンプトは**どのプロファイルでも外しません**。速さのために読めない
  文字起こしを作るのは速さではありません。
- **標準の「2」は測定結果ではありません。** このリポジトリには日本語の参照文字起こしが
  なく、CER は測定していません。ビームサーチのコストはビーム幅にほぼ線形、
  利得は急速に飽和するため「貪欲から 1 段」が最初の意味のある点である、という
  設計上の判断です。実音声での比較手順は README「処理時間を測る」にあります。
- 高速で話者分離を必ず切るのは、話者分離が「省ける処理」のうち最も明確なものだから
  です。**マイク／PC音声の区別は影響を受けません** — これはモデルの推測ではなく、
  どちらのファイルから読んだかという事実です。

### 3.6.2 計測

`TranscriptionMetrics` が 1 回のパスの内訳を保持し、ログへ 1 ブロックとして書きます
（README「処理時間を測る」に出力例）。話者分離の時間は認識時間と**別に**測ります。
分けなければ「話者分離を切ると速いのか」という質問に答えられないためです。

会議長は各系統の**合計ではなく最大**です。マイクとループバックはどちらも会議全体に
またがるため、足すと 10 分の会議が 20 分になり、そこから導く比率がすべて半分になります。

`tools\Show-Performance.ps1` がログ中の全実行分を表にします。

### 3.7 モデル選定と性能自動調整

ユーザーは「高速／標準／高精度」を選びません。既定値はこの端末の実測から決まり、
**設定画面でいつでも上書きできます**。

1. `CapabilityProbe` が **実測**します（CPU 型番は使いません）。
   96×96 の密行列積を 1 スレッド／推奨スレッド数で実行し、
   ウォームアップ後のスループットを i5-1335U の P コア相当 = 1.0 として正規化。
2. `ProfileSelector` が **梯子の上から降りてきます**。以前は下から登っていました。
   録音に追いつく必要がなくなったため、判断基準が
   「間に合うか」から「**このPCが保持できて、待てる時間で終わる中で最も精度が高いのはどれか**」
   に変わったためです。
3. 自動選択の上限は `AcceptableProcessingFactor = 1.5`（1時間の録音に約1時間半）。
   これを超えるモデルは**自動では選ばれません**が、設定画面には常に全モデルが並び、
   選べば推定処理時間が表示されます。
4. RAM が「モデル所要量 × 3」に満たない場合は選びません（Windows・ブラウザ・
   会議クライアントと共存させるため）。

**実行時の機能縮退（degradation ladder）は削除しました。** あれは「リアルタイムに
追いつけないときに何を諦めるか」という仕組みであり、追いつく相手が存在しません。
処理が長いことは障害ではなく、ユーザーが選んだ待ち時間です。

### 3.8 モデル候補

| id | ファイル | サイズ（実測バイト） | 用途 |
| --- | --- | --- | --- |
| `whisper-tiny-q5_1` | `ggml-tiny-q5_1.bin` | 32,152,673 | 最軽量 |
| `whisper-base-q5_1` | `ggml-base-q5_1.bin` | 59,707,625 | 軽量 |
| `whisper-small-q5_1` | `ggml-small-q5_1.bin` | 190,085,487 | バランス |
| `whisper-medium-q5_0` | `ggml-medium-q5_0.bin` | 539,212,467 | 高精度 |
| `whisper-large-v3-turbo-q5_0` | `ggml-large-v3-turbo-q5_0.bin` | **574,041,195** | 最高精度 |

サイズと SHA-256 は `.github/workflows/model-hashes.yml` が配布元から実際に
ダウンロードして測定した値です。**推測した値は1つもありません。**

**large-v3-turbo を追加した理由。** large-v3 のエンコーダー（32層 / d=1280）を
そのまま持ち、デコーダーが4層に蒸留されているモデルです。whisper.cpp の CPU 実行では
エンコーダーが支配的なので「large 級のエンコーダーを、この階級のマシンで人が待てる
時間内に回す」唯一の現実的な選択肢になります。リアルタイム制約があったときには
検討する意味すらありませんでした。

**互換性の確認方法。** Whisper.net 1.8.1 が同梱する whisper.cpp が turbo 形式に
対応していることは、`.github/workflows/build-windows.yml` の `evaluate_models`
入力で実際にモデルをダウンロードし、製品と同じデコード設定で推論させて確認しています。
依存パッケージのバージョンは変更していません。

**実測して分かったこと（turbo は medium より速くはありません）。**
windows-latest ランナー（4論理コア / 2スレッド）で small・medium・turbo を
同一音声に対して実行した結果、推論時間は 12.17 / 38.67 / 66.36 秒でした
（TEST_RESULTS.md 2.15）。turbo は medium の **1.72 倍の時間**がかかります。
turbo が「速い」のは large-v3 と比べての話であり、medium と比べてではありません。
したがって **turbo を無条件に既定にはしていません。**既定は `ProfileSelector` が
実測スコアから「音声長の 1.5 倍以内で終わる見込みか」で決めます。turbo が
自動選択されるのは実測スコアが 1.1 × 7.4 / 1.5 ≒ 5.43 以上のときだけで、
2026-09-08 に確認した実機（スコア 1.34）では small が選ばれました。ピークワーキングセットは turbo 1,085 MB /
medium 1,364 MB で、どちらも 16GB 環境に収まりました。

**採用しなかった量子化。** 同時に測定した q8_0（874,188,075 バイト）と
f16（1,624,555,275 バイト）はカタログに載せていません。16GB の環境で Windows・
ブラウザ・会議クライアントと共存させる余裕が乏しく、`RequiredRamMb × 3` の
条件を満たさないためです。

**未実測であること。** 基準PC（Core i5-1335U / RAM 16GB）での実処理時間と
メモリ使用量は測定していません（測定したのは CI ランナーであって基準PCではありません）。
`RelativeCost` はエンコーダーの計算量（層数 × 次元²）から導いた**推定値**です。

```
small        12 層 × 768²  =  7.08M  → 1.0（基準）
medium       24 層 × 1024² = 25.17M  → 3.6
large-v3-turbo 32 層 × 1280² = 52.43M → 7.4
```

デコーダーを意図的に無視しているため、**large-v3-turbo にとっては悲観的**な値です
（turbo はデコーダーが 4 層しかありません）。補正するにはその会議のトークン数と
フレーム数の比を仮定する必要があり、そちらのほうが根拠が弱くなります。
悲観側に外すほうが安全でもあります — 自動選択が保守的になるだけで、
ユーザーが想定より長く待たされることはありません。実測でもそうなりました:
推定比 7.4 / 3.6 = 2.06 に対し、ランナーでの実測比は 1.72 でした（推定が約 20% 重め）。
ハードウェア 1 種類の実測に合わせて定数を書き換えることはしていません。

この値が影響するのは**既定のモデル選択と、設定画面に表示する目安**だけです。
どのモデルも手動で選べますし、UI 上も「実測値ではなく推定です」と明示しています。

### 3.9 GPU / OpenVINO

**GPU は必須にしていません。CPU が唯一の必須経路です。**

Intel Core i5-1335U + 内蔵 GPU という想定環境に対して OpenVINO を検討しましたが、
**今回は採用していません。** 理由は性能ではなく配布形態です。

| 障害 | 内容 |
| --- | --- |
| ランタイムの同梱 | Whisper.net の OpenVINO 経路は `Whisper.net.Runtime.OpenVino` と OpenVINO ランタイム本体を必要とし、配布 ZIP が数百 MB 増えます |
| モデル変換 | whisper.cpp の OpenVINO エンコーダは ggml ファイルとは別に、変換済みの IR（`.xml` / `.bin`）を必要とします。モデルカタログは Hugging Face の ggml をそのまま取得する設計なので、変換工程か第2の配布物が要ります |
| ドライバ依存 | iGPU 推論は Intel Graphics ドライバのバージョンに依存し、動作 PC が事実上限定されます |
| 効く範囲 | 高速化されるのはエンコーダーだけで、デコーダー（ビームサーチ）は CPU のままです |

いずれも「portable 動作」「管理者権限不要」「動作PCを限定しない」という
本製品の前提と正面から衝突します。したがって**まずソフトウェア設定側**
（窓の充填、ビーム幅、温度フォールバック、話者分離、議事録の推論回数）を
最適化する方針を採りました。§3.4 の実測（エンコーダー換算 5.00 倍 → 1.25 倍）は、
その判断が正しかったことを示しています。

将来 OpenVINO を採用する場合も、**CPU フォールバックは必ず残します。**

---

## 4. 話者分離（実験的）

**実行タイミング: 文字起こし中のみ。録音中には実行しません。**

- 各発話区間から **MFCC 1〜12 の平均と標準偏差（24次元）** を作り、L2 正規化。
- 既存クラスタとのコサイン類似度が **0.82** 以上なら同一話者、なければ新規クラスタ。
- クラスタはキャプチャストリームごとに独立（マイク側と PC 音声側で番号を共有しない）。
- クラスタ上限 8。

**限界を明記します。** これはニューラル話者埋め込みではありません。
重なった発話は分離できず、声質の似た2人は統合され、
マイクとの距離が変わった同一人物は分割されることがあります。
そのため:

- 発言元の「マイク / PC音声」は **常に**録音元ストリームで決定され、話者分離の成否に依存しません。
- 人物名を AI が推測することはありません。`Speaker 1`, `Speaker 2` のみを付与し、
  実名はユーザーが入力した場合のみ表示されます（会議内の同一話者へ一括反映）。
- **インポートした音声には適用しません。** 1本の音声には音源の情報がないため、
  クラスタリングした結果に意味づけできる根拠がありません。UI 側でも
  「話者名を編集」を無効化し、理由を表示します。
- この限界は README.md の「既知の制約」にも同じ表現で記載しています。

---

## 5. 議事録生成（実験的・要件44-47）

2つの生成器があります。

| 生成器 | 依存 | 動作 |
| --- | --- | --- |
| `ExtractiveMinutesGenerator` | なし（常に利用可能） | 決定・アクション・継続検討のキューフレーズに一致する文を **原文のまま**タイムスタンプ付きで抽出。10分ブロックごとに議題を整理 |
| `LocalLlmMinutesGenerator` | llama.cpp + GGUF | 収まればシングルパス 1 回、収まらなければ map N 回 + 構造化 reduce 1 回。失敗時は抽出型へフォールバック |

共通の方針:

- **文字起こし結果が存在する場合にのみ、ユーザーがボタンを押したときだけ実行**します。
  録音しただけの状態ではコマンドが無効です。議事録は文字起こしの本文から作るもので、
  本文がなければ作れるものが何もないためです。
- 出力の骨格は `MinutesTemplate` が組み立てるため、モデルが脱線しても6セクションは必ず存在します。
- プロンプトは「文字起こしにない情報を書かない」「担当者・期限が明記されていなければ
  必ず `要確認`」「人物名を推測しない」を明示。
- 進捗は実際に処理したブロック数（偽の進捗バーではありません）。

### 5.1 推論回数はトークン数が決めます

以前は会議の長さにかかわらず 1500 文字ずつのブロックに分割し、map のあとに
概要・決定事項・ToDo・継続検討を**別々のプロンプト**で生成していました。
10 分の会議でも **N + 4 回**の推論です。CPU 推論では出力トークン数が支配的で、
この構成は着手前に N×320 + 1120 トークンの復号を確定させていました。

現在は `MinutesPlanner` が**モデル自身のトークナイザ**
（`LLamaWeights.Tokenize`）で数えて決めます。

| 条件 | 方式 | 推論回数 |
| --- | --- | --- |
| 文字起こし + プロンプト + 出力枠 ≦ コンテキスト長 × 0.88 | シングルパス | **1** |
| 収まらない | map + 構造化 reduce | **N + 1** |

- 安全マージンは **12%**。チャットテンプレートが加えるロールマーカーはこのコードから
  見えず、あふれたときの llama.cpp の挙動は「プロンプトの先頭を黙って捨てる」です。
  会議の後半だけを要約して、そうと言わない議事録ができます。
- モデルが読み込めずトークナイザが使えない場合は `HeuristicTokenCounter`
  （1 文字 = 1 トークン）に落ちます。日本語では**過大評価**になるため、
  判定は map-reduce 側に倒れます。過大評価は推論が 1 回増えるだけ、
  過小評価はコンテキスト溢れなので、倒す向きはこちらです。
- 分割は 1500 文字固定ではなく**収まる範囲で最大**。文字起こしの行の途中では切りません
  （`NoTranscriptLineIsEverSplitAcrossTwoBlocks` が結合して原文と一致することを検証）。

### 5.2 構造化出力とパース

6 セクションを 1 回の推論で生成し、`MinutesResponseParser` が振り分けます。

JSON ではなく `【決定事項】` のような見出し行を使っています。1.5B の instruct モデルに
CPU で JSON を書かせると、出力トークンが句読点に費やされ、それでも末尾カンマが
無視できない頻度で出ます。**波括弧 1 つの失敗が応答全体を失わせる**のに対し、
見出し 1 つの欠落はセクション 1 つの損失で済みます。

**パース失敗で議事録全体が失われない設計**です。

| 状況 | 挙動 |
| --- | --- |
| 最初の見出しの前に前置き | 「概要」に入れる（散文の置き場所として妥当） |
| 想定外の見出し | セクション境界として扱わない。直前のセクションに残るので文書からは消えない |
| 見出しが 1 つもない | 応答全体を「概要」に入れ、その旨を「補足」に記載 |
| 見出しの装飾（`## X` / `**X**` / `X:`） | いずれも同じ見出しとして認識 |

推測禁止のルールは変更していません。担当者・期限は**常に** `要確認` です。
自由文から拾った担当者名は、間違っていたときに議事録として最悪の失敗になるためです。

### 5.3 計測

`MinutesMetrics` が推論回数・map/reduce 別の時間・入出力トークン数・
tokens/sec をログへ 1 ブロックとして書きます（README に出力例）。
推論回数をトークン数と分けて数えるのは、「モデルが遅い」と
「回数を呼びすぎている」を区別できるようにするためです。

---

## 6. 永続化とクラッシュ復旧（要件50-51）

アプリで録音した会議:

```
2026-09-07_1430_Meeting/
├── meeting.wav              逐次書き込み（ヘッダを毎フラッシュで更新）
│                            停止後に一定ゲインを1回適用（一時ファイル経由で置換）
├── recognition-mic.wav      16kHz 系統別。文字起こしの入力
├── recognition-system.wav   同上
├── transcript.txt / .md     停止時・文字起こし完了時・編集時に生成
├── minutes.txt / .md        「議事録を生成」実行時
├── metadata.json            原子的書き込み（.tmp → Replace）
└── session.journal          録音中のみ存在。追記専用 JSON Lines
```

インポートした音声:

```
2026-09-07_1512_interview/
├── recognition-import.wav   元ファイルから復号した 16kHz 作業音声
├── transcript.txt / .md
├── minutes.txt / .md
└── metadata.json            isImported / importedFromPath を含む
```

**インポート元のファイルはコピーしません。** 会議フォルダーに入るのは作業音声だけで、
音声の本体はユーザーが置いた場所に残ります。`metadata.json` にパスを記録しているため、
作業音声を削除したあとでも、元ファイルが同じ場所にあれば作り直して再文字起こしできます。

ファイル名は従来の仕様と後方互換です。`.stt-spill/` は
（それを必要としていた仕組みごと）廃止されました。

- **WAV ヘッダ自己修復**: フラッシュのたびに RIFF/data サイズを実書き込み量で書き直します。
  プロセスが強制終了しても、残ったファイルはそのまま再生できます（修復ツール不要）。
- **追記専用ジャーナル**: 認識結果・編集・進捗を1行1JSONで追記。
  電源断で最終行が途中で切れても、その行を読み飛ばすだけで直前までが完全に復元されます
  （テスト `JournalReplayReconstructsEverySegmentEvenAfterATornLastLine`）。
- **起動時スキャン**: 保存先に「完了していないジャーナル」を持つフォルダがあれば
  復旧ダイアログを表示します。「復旧しない」を選んでも音声ファイルは削除しません。
- **作業音声の削除条件**: `RecognitionAudioCleanup` が唯一の判断箇所で、
  規則は「ユーザーが削除を選んでいて、**かつ**文字起こしが正常完了した場合のみ」です。
  失敗・中止では削除しません。次にやることは「もう一度試す」であり、入力を消すと
  それができなくなるためです。アプリ録音の系統別音声は**どこからも復元できません**。

---

## 7. デバイス変更とスリープ（要件52-53）

- キャプチャが予期せず停止した場合、最大 5 回・線形バックオフ（0.5s, 1.0s, ...）で
  デバイスを再解決して再接続します。成功／失敗のいずれも `CaptureFault` として
  UI に日本語で通知します。**自動復帰できない場合は明示します**（要件52）。
- スリープ抑制は `SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED)` を
  **専用スレッド**から呼びます。スレッドプールから呼ぶとスレッド回収時に要求が失われます。
  `ES_DISPLAY_REQUIRED` は要求しません（画面ロックポリシーを上書きしない）。
  プロセス終了時に自動解除されるため、クラッシュしても設定は残りません。管理者権限は不要です。

---

## 8. セキュリティとプライバシー

| 項目 | 実装 |
| --- | --- |
| 会議データの外部送信 | 実装上存在しません。HTTP クライアントは `ModelDownloader` の1箇所のみ |
| インポートした元ファイル | 読み取りのみ。変更・移動・削除・送信のいずれも行いません（テスト `TheImportedFileIsNeverModifiedMovedOrDeleted`） |
| クラウド STT / LLM API | 依存関係に含まれません（`Whisper.net` と `LLamaSharp` はいずれもローカル推論） |
| 許可される通信 | AIモデルのダウンロードのみ。URL は `ModelCatalog` のハードコード許可リストに限定され、HTTPS 以外は拒否。任意 URL 入力欄は存在しません |
| 整合性 | SHA-256 を必ず計算。カタログにピン留めハッシュがあれば照合し、不一致ならファイルを削除して失敗。ピン留めが無い場合は「照合できなかった」ことを明示し、計算値をローカル `models.json` に記録（以後の改竄検出に使用） |
| テレメトリー | Analytics / Application Insights / Sentry いずれも不使用。クラッシュログの自動送信なし |
| ログ | ローカルのみ。会議本文や文字起こし全文はログに複製しません（イベント・件数・処理時間のみ） |
| 権限 | `app.manifest` は `asInvoker` |

---

## 9. 配布とビルド

- `dotnet publish -r win-x64 --self-contained true` による自己完結型配布。
  .NET / VC++ / Python / Node.js の事前インストールは不要です。
- **トリミングは無効**: WPF はトリム非対応であり、Whisper.net と LLamaSharp は
  ネイティブアセットと型をリフレクションで解決します。ZIP がやや大きくなる代わりに
  「展開したら動く」ことを優先しました。
- **単一 EXE にはしていません**: 要件でも必須とされておらず、
  単一ファイル化はネイティブライブラリの展開を伴い起動が遅くなるためです。
- **llama.cpp の命令セット別レイアウトを保持しています**: LLamaSharp は
  `noavx / avx / avx2 / avx512` の 4 種のネイティブバイナリを同梱し、
  実行時に上から順にロードを試みます。RID 指定の publish は既定で
  `runtimes/<rid>/native/` を出力ルートへ平坦化するため、
  4 種が同名で衝突して `NETSDK1152` になります。
  仮に平坦化に成功しても危険です — 1 種類しか同梱されず、
  AVX-512 非対応の PC で AVX-512 ビルドを読み込むと
  「不正な命令」で即座に落ちます。そのため publish ターゲットで
  元のパッケージ内パスへ戻し、CI で 4 種の存在とルートに
  `llama.dll` が無いことを検証しています。
- CI は `windows-latest` 上で restore → build → test → publish → ZIP → artifact を実行し、
  publish 出力に `MeetingRecorder.exe` / ランタイム / whisper ネイティブが存在すること、
  および **AIモデルが混入していないこと** を検証します。
- `build-windows.yml` の手動実行では `evaluate_models` 入力に ggml ファイル名を渡せます。
  指定したモデルを実際にダウンロードし、製品と同じデコード設定で推論させ、
  サイズ・SHA-256・読み込み時間・ワーキングセット・処理時間を出力します。
  **カタログにモデルを追加する前に、実際に動くことを確かめるための入口です。**
  空（既定）のときは通常のビルドと変わりません。
- **WPF を含む完全なビルドは Windows でしか行えません。** Linux では
  `Core` / `Stt` / `Diarization` / `Minutes` / `Audio` とそのテストのみビルド・実行できます
  （`.NET SDK` の WindowsDesktop コンポーネントが非 Windows 版に含まれないため）。
  したがってアプリ本体のコンパイル確認は CI が唯一の手段です。

---

## 10. CI で検証できないこと

`docs/WINDOWS_E2E_TEST.md` と `TEST_RESULTS.md` に一覧があります。要点のみ:

GitHub-hosted の Windows ランナーには実マイク・実スピーカー・実オーディオセッションが
ありません。したがって **CI のビルド成功は WASAPI 実機動作の確認を意味しません。**
実機確認が必要な項目（マイク録音、ループバック取得、Zoom/Meet/Teams 実音声、
長時間録音、CPU/RAM 実測、SmartScreen、i5-1335U での文字起こし実性能）は
すべて「Windows 実機確認が必要」として分類しています。

この改修で追加された未検証項目:

| 項目 | 状態 |
| --- | --- |
| 基準PC（i5-1335U）での medium / large-v3-turbo の実処理時間・RAM | **未実測**。設定画面の表示は推定値であることを明示 |
| 生の ADTS `.aac` ファイルのインポート | **未検証**。CI で真の ADTS を生成する手段がないため。MP4 コンテナの AAC（`.m4a`）は往復テスト済み |
| Windows N / KN での MP3 / M4A / AAC インポート | **未検証**（Media Feature Pack 非導入環境が CI にない）。WAV は OS コーデックに依存しないため影響を受けません |
| 日本語会議音声での実認識精度 | **未測定**。CI の合成音声は英語（Windows ランナーに日本語音声がないため） |
