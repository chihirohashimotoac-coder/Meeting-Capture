<Application x:Class="MeetingRecorder.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Japanese-first typography; Yu Gothic UI is present on Windows 10/11. -->
            <FontFamily x:Key="UiFont">Yu Gothic UI, Meiryo UI, Segoe UI</FontFamily>

            <SolidColorBrush x:Key="PanelBrush" Color="#F5F6F8" />
            <SolidColorBrush x:Key="BorderBrush" Color="#D8DBE0" />
            <SolidColorBrush x:Key="AccentBrush" Color="#1B6EC2" />
            <SolidColorBrush x:Key="WarningBrush" Color="#FFF4CE" />
            <SolidColorBrush x:Key="WarningBorderBrush" Color="#E8B923" />
            <SolidColorBrush x:Key="MeterBackgroundBrush" Color="#E3E6EA" />
            <SolidColorBrush x:Key="MeterFillBrush" Color="#2E9E4F" />
            <SolidColorBrush x:Key="MutedTextBrush" Color="#5A6069" />

            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Background" Value="White" />
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="SectionHeader" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="0,0,0,4" />
                <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            </Style>

            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
                <Setter Property="Padding" Value="14,6" />
                <Setter Property="MinWidth" Value="96" />
                <Setter Property="Margin" Value="0,0,8,0" />
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
                <Setter Property="Padding" Value="4,3" />
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
                <Setter Property="Padding" Value="4,3" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
